<h1>My Profile</h1>
<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<%= image_tag(@user.profile_picture) if @user.profile_picture.attached? %>

<br>

<%= link_to 'Edit', edit_user_path(@user) %>